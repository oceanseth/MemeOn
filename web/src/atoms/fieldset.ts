/* Transitional path: the parts live in `@/atoms/field` as `FieldSet` / `FieldLegend`. The screen
   wave moves the one consumer (TradesScreen); E1 deletes this file. */
export { Fieldset, FieldsetLegend } from '@/atoms/field'
